void r(double, void);
void s(int);

double d;

int f(short i) {
    f(d);
    1 = 2;
    -s(1);
    d = f(1) && d;
}

void f(void) {}

double x = y;

double dbl(int i, double d) {
    s(i, d);
    dbl(s(i), d); dbl(i);
    d >= und3f | und3f && und3f % und3f;
    und3f + und3f;
    x = s(i) + 1;
    if(x) {}
}

void ret(void) {
    return d;
}